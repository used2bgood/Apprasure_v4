<nav class="site-nav" aria-label="Main">
  <div class="container">
    <button class="nav-toggle" aria-controls="nav-links" aria-expanded="false" id="navToggle">
      <span class="nav-toggle-bar"></span>
      <span class="nav-toggle-bar"></span>
      <span class="nav-toggle-bar"></span>
      <span class="sr-only">Menu</span>
    </button>

    {% set items = [
      { href: '/',                    label: 'Home' },
      { href: '/#how-it-works',       label: 'Why Appraisal?' },
      { href: '/pages/services/',     label: 'Services' },
      { href: '/pages/testimonials/', label: 'Testimonials' },
      { href: '/pages/contact/',      label: 'Contact' }
    ] %}

    <div id="nav-links" class="nav-links">
      {% for it in items %}
        <a href="{{ it.href }}" class="{% if page.url == it.href %}active{% endif %}">{{ it.label }}</a>
      {% endfor %}
    </div>
  </div>
</nav>
